[System: your previous response contained tool call syntax written as plain text instead of using the tool calling API. Your text output is sent directly to users — use the `reply` tool to send a message, `react` to add a reaction, or `skip` to stay silent. Try again.]
